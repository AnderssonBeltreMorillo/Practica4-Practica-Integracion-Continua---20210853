name: Notificacion NTFY

on:
  push:
    branches:
      - main

jobs:
  enviar-alerta:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar mensaje a ntfy.sh
        run: |
          curl -d "Nuevo codigo subido a la rama main" https://ntfy.sh/devops-itla
